const handleHome = (req, res) => {
    res.send('<H1> Welcome to Home </H1>');
};

const handleContact = (req, res) => {
    res.send('<H1> Welcome to Home </H1>');
};

module.exports = {
    handleHome,
    handleContact
}
